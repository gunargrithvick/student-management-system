# Copy to .env and fill in real values. Never commit .env.

# --- Server ---
PORT=3000
NODE_ENV=development

# --- Database ---
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=student_app
DB_PASSWORD=change-me
DB_NAME=student_management
# Used by docker-compose for the MySQL root account.
DB_ROOT_PASSWORD=change-this-too
# Set to true when connecting to a managed MySQL provider over TLS.
DB_SSL=false
# Optional PEM CA certificate for managed MySQL providers such as Aiven.
# DB_SSL_CA=

# --- Auth ---
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-string-at-least-32-chars
# How long a login session lasts.
SESSION_TTL=8h

# --- Bootstrap admin (used only by `npm run init-db`) ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-before-deploying
